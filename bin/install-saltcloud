#!/bin/bash
set -eu
sudo add-apt-repository -y ppa:saltstack/salt
sudo apt-get update
DEBIAN_FRONTEND=noninteractive sudo apt-get install -y salt-cloud

echo
echo
echo "install complete - fill in creds and master host in /etc/salt/cloud"
echo
echo
